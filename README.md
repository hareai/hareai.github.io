# hare

`still lost, still going.`

Personal site. Built with [lonefox](https://github.com/hareai/lonefox).

Wordmark on this preview is a trial: `hare`. Motto stays.

## Develop

```sh
npm install
npm run dev -- --host
npm run build
```

Copy lives in `lonefox.config.ts`. Posts live in `src/content/blog/`.
